import { SafeAreaView, Text } from "react-native";

const UserName = () => {
    return (
        <SafeAreaView>
            <Text className="text-2xl font-size">UserName</Text>
        </SafeAreaView>
    );
};

export default UserName;
