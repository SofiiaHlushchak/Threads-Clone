import { SafeAreaView, Text } from "react-native";

const Auth = () => {
    return (
        <SafeAreaView>
            <Text className="text-2xl font-size">Auth</Text>
        </SafeAreaView>
    );
};

export default Auth;
